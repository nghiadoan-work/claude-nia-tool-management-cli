# CNTM Environment Configuration
# This file allows you to configure the registry and authentication for cntm

# Registry Configuration
# The GitHub repository URL for the tool registry
CNTM_REGISTRY_URL=https://github.com/nghiadoan-work/claude-tools-registry

# The branch to use in the registry (usually 'main' or 'master')
CNTM_REGISTRY_BRANCH=main

# GitHub Personal Access Token (required for private registries)
# Generate a token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
# CNTM_REGISTRY_TOKEN=your_github_token_here

# Local Configuration
# Default path for .claude directory (default: .claude)
# CNTM_DEFAULT_PATH=.claude

# Enable automatic update checks (default: true)
# CNTM_AUTO_UPDATE=true

# Publishing Configuration
# Default author name for published tools
# CNTM_DEFAULT_AUTHOR=Your Name

# Automatic version bump strategy: patch, minor, major
# CNTM_AUTO_VERSION_BUMP=patch

# Notes:
# - Lines starting with # are comments
# - Remove the # to uncomment and activate a setting
# - Changes take effect immediately (no restart needed)
# - This file should NOT be committed to version control if it contains tokens
# - Add .cntm.env to your .gitignore file
